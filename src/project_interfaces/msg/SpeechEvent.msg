string speaker_id      # "unknown" oppure l'ID dell'utente (es. "1", "2")
string transcription   # Il testo detto dall'utente
float32[] embedding    # Vettore (embedding) della voce per la registrazione
